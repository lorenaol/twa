import {User} from "@app/entities/user";

export class Anunt {
  id?: number;
  user?: User;
  detalii?: string;
  // tip?: string;
  materie? : string;
  telefon? : string;
  oras?: string;
  image?: string;
  facultate? : string
  an? : string
  clasa?: string
}
